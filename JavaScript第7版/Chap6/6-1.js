/**
 * オブジェクト
 * ・文字列を値にマッピングするもの
 * ・プロパティ名をシンボルにすることが可能
 * ・他のオブジェクトからプロパティの継承をすることが可能（プロトタイプ継承）
 * ・値ではなく参照で操作
 * ・独自プロパティを持つ（継承されていないプロパティを参照するためのもの）
 * 　・writable属性
 * 　・enumerable属性（デフォルトでは可能）
 * 　・configurable属性
 */